THE_ARK_CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Server only. Copy these to the ignored root .env.local; never use VITE_ for keys.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-6
ANTHROPIC_WORKSPACE_ID=
